# Translation Boundary Stub

This module stays intentionally light in the first slice.

Phase 1 needs the workflow to be translation-aware, but not a full translation workspace yet.
Approval hooks and state already exist in the core schema so the module can grow without
rewiring the workflow model.
