# Profiles And Templates

This boundary owns profile-to-template mapping for `SHAWN`, `SOPHIAN_YACINE`, and `Zazmic job posts`.

The first slice seeds the model and keeps the mapping rules explicit so later Canva execution
and future automation hooks can stay deterministic.
